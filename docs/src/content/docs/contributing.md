---
title: "Development"
description: "Working on keel itself: install, type-check, test, pack."
---

```bash
pnpm install       # `prepare` compiles the package
pnpm check-types   # tsc over sources and tests
pnpm test          # vitest
pnpm pack          # what would actually ship
```
